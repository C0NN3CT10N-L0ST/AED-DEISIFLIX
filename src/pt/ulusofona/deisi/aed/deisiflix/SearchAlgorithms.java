package pt.ulusofona.deisi.aed.deisiflix;

import java.util.ArrayList;

public class SearchAlgorithms {
    // Binary Search Algorithm for 'PeopleID' ArrayList
    /*
    static int binarySearchActorByID(ArrayList<ActorID> people, int id) {
        int left = 0, right = people.size() - 1;

        while (left <= right) {
            int middle = (left + right) / 2;

            if (people.get(middle).id == id) {
                return middle;
            }

            if (people.get(middle).id < id) {
                left = middle + 1;
            } else {
                right = middle - 1;
            }
        }
        return -1;
    }
    */
}
